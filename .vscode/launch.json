{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Crypto Bot",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",  // This should point to the directory containing all your Go files
            "envFile": "${workspaceFolder}/.env",
            "args": []
        }
    ]
}
